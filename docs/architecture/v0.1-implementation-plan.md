# v0.1 実装計画と完了状況

> 状態: v0.1.0 の実装済み計画。自動検証完了、Chrome 実機 smoke と CWS Release Gate は保留。
>
> 対応設計: [v0.1 design](v0.1-design.md) / [ADR 0001](../adr/0001-experimental-chatgpt-web-handoff.md)

この文書は、Accepted Experimental design を小さくレビュー可能な変更へ分割した計画と、現在の実装・検証状況を記録します。未完了の実機確認や CWS 提出を完了扱いにしません。

## Step status

| Step | 内容 | 状態 | 主な実装 / 検証 |
| --- | --- | --- | --- |
| 0 | 実装ゲート、型境界、Chrome minimum | 完了 | `src/state/types.ts`、Manifest v3、Chrome 116、manifest test |
| 1 | Settings / consent page と action | 完了 | `src/settings/`、Service Worker message contract、settings tests |
| 2 | Context menu router と X selection input | 完了 | `service-worker.ts`、selection menu、X document pattern、integration test |
| 3 | X URL normalizer と fixture | 完了 | `src/core/url.ts`、`src/sources/x/`、URL / DOM fixture tests |
| 4 | Prompt builder と session payload | 完了 | `src/core/prompt.ts`、`src/storage/session-store.ts`、prompt / session tests |
| 5 | State machine と一度だけの claim | 完了 | `src/state/machine.ts`、request ID 直列化、state / integration tests |
| 6 | ChatGPT Web adapter | 完了 | `src/destinations/chatgpt/`、selector / adapter tests。実機 DOM は未確認 |
| 7 | Offscreen clipboard fallback | 完了 | `src/offscreen/`、`src/handoff/fallback.ts`、clipboard / fallback tests |
| 8 | 統合検証と unpacked smoke | 自動検証完了・実機保留 | test / typecheck / lint / build / secret scan 完了。Chrome 実機 smoke 未完了 |
| 9 | Release Gate / CWS | 保留 | 正式名称、素材、Privacy URL、Security / Privacy review、実機確認、CWS 提出が未完了 |

## 完了した実装の要点

### Step 0–1: 型、Manifest、Settings / consent

- `PendingPayload` と `Settings`、`awaitingConsent | queued | injecting` を定義しました。
- Manifest は v0.1.0、minimum Chrome 116、required `activeTab` / `contextMenus` / `scripting` / `storage`、optional `offscreen` / `clipboardWrite` / `https://chatgpt.com/*` です。
- action は設定ページを開き、設定ページは通常設定と request ID 付き preview の二つの mode を持ちます。
- preview は exact prompt、URL、selection、宛先、DOM automation / clipboard / no-retry の注意を表示します。
- UI は storage API を直接扱いません。approve button の同期 click handler が user gesture を保ったまま `chrome.permissions.request()` で optional bundle を直接要求し、要求の promise 解決後に approve runtime message を送ります。Service Worker は message を受けた後に `chrome.permissions.contains()` で `https://chatgpt.com/*`、`offscreen`、`clipboardWrite` の一式を最終確認し、不足・拒否なら pending を削除します。storage 操作は Service Worker 経由です。

### Step 2–4: X input、URL、prompt、pending

- context menu は X / Twitter の `selection` context に一項目だけ登録します。
- X DOM extractor は選択位置に近い article 内 status link を距離・DOM 順で選びます。
- URL は HTTPS、許可 host、status id を検証し、query/hash と suffix を除去します。fallback page URL も同じ normalizer を通します。
- selection は LF 化、trim、最大 8,000 UTF-16 code units です。
- 固定 prompt の動的値は sanitized URL と selection だけで、未信頼データ境界と非実行の注意を含みます。
- pending payload は `storage.session` に保存し、`expiresAt` の10分到達で論理失効させます。`alarms` は使わないため、期限到達だけで物理削除されるとは限らず、次の Service Worker 起床・関連イベント、または browser restart などで物理削除されます。終端イベントでは削除し、settings / consent version だけを `storage.local` に保存します。

### Step 5: State machine と tab race

- request ID ごとの Service Worker operation を直列化し、`queued` → `injecting` を `claimId` 付きで一度だけ保存します。
- consent tab は `about:blank` 作成 → `consentTabId` と state 保存 → extension settings URL 遷移です。
- ChatGPT target tab は `about:blank` 作成 → claim / `targetTabId` と state 保存 → `https://chatgpt.com/` 遷移です。
- adapter 直前に `adapterAttemptedAt` を保存し、Service Worker restart、duplicate event、tab close、expiry でも retry しません。

### Step 6–7: ChatGPT adapter と clipboard fallback

- selector registry、isolated-world adapter、bounded MutationObserver、timeout、send button 一回操作を実装しました。
- 未ログイン、selector mismatch、timeout、send unknown、executeScript failure は offscreen clipboard write と固定 banner に終端化します。
- clipboard 失敗でも pending は残さず、既存 clipboard は読みません。banner に selection / prompt を再掲しません。

## Step 8 の自動検証

2026-08-24 時点で次を実行済みです。

```text
npm test                 41 tests passed
npm run typecheck        passed
npm run lint             passed
npm run build            passed
npm run check:secrets    passed
```

`npm run build` は Service Worker、settings、offscreen の bundle と静的ファイルを `dist/` に生成します。`dist/` は生成物であり、コミットしません。

## Step 8 の実機 smoke（保留）

Chrome 116 以上で、個人情報を含まないテスト文を用いて次を確認します。

- X / Twitter の selection menu、status link、page URL fallback
- 初回 exact preview、permission grant / deny、拒否時の cleanup、同意撤回
- ChatGPT logged-in / logged-out、foreground default / background setting
- composer / send selector、DOM 変更、timeout、送信結果不明、二重送信なし
- clipboard success / failure、banner、target / consent tab close、Service Worker reload

実機で問題が見つかった場合は、DOM adapter の変更を [ADR 0001](../adr/0001-experimental-chatgpt-web-handoff.md) の scope 内でレビューし、設計・Privacy・Security・CWS 文書を同期します。

## Step 9 Release Gate（保留）

| Gate | 状態 | 完了条件 |
| --- | --- | --- |
| 実機検証 | 保留 | Chrome 116+ の上記 smoke とログ・fixture の機密情報確認 |
| Security / Privacy review | 保留 | 権限、third-party ChatGPT 境界、clipboard、削除、secret scan の最終確認 |
| Branding / listing | 保留 | 正式名称、商標、説明、カテゴリ、アイコン、スクリーンショット |
| Privacy Policy | 保留 | 公開 URL、連絡先、CWS data disclosure と [PRIVACY.md](../../PRIVACY.md) の一致 |
| Support / vulnerability | 保留 | GitHub Issues と Private vulnerability reporting の案内 |
| CWS 提出 | 保留 | 上記完了後にのみ提出。現在は未公開 |

実装済みであることだけを理由に Chrome Web Store へ提出しません。Experimental DOM automation の実機結果と公開 gate の承認を先に確認します。
